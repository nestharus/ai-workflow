"""Test analysis tools for querying coverage data.

These tools provide LLM-friendly access to coverage data stored in the
.coverage/coverage.db SQLite database, applying the same exclusion rules
as test_coverage.py validation.
"""
