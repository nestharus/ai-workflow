"""Planner evaluation package.

Evaluates planner decisions against constraint-based ground truth,
computes a PlannerScorecard (separate from the pipeline Scorecard),
and provides trace analysis tooling (load, filter, replay, diff).
"""
