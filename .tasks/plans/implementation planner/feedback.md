We do not do test coverage by branch/line/use-case
We do test coverage by invariants
We will not have tests for trivial things like specific exception handling for ecosystem libs
We will have high signal tests for the actual artifact
This prevents test spam
Keeps test runs faster