# Lint script configuration for hadolint
# Directories to exclude when searching for Dockerfiles

exclude_dirs:
  - .tasks/store  # Generated task files
  - .venv
  - .venv2
  - docs/plans
  - docs/references
  - to_adapt
