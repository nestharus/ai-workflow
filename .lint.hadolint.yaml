# Lint script configuration for hadolint
# Paths to include for Dockerfile scanning (glob patterns)

included_paths:
  # Root-level Dockerfile
  - "Dockerfile"
  # Scripts directory
  - "scripts/**/Dockerfile"
  # App directory
  - "app/**/Dockerfile"
  # Tests directory
  - "tests/**/Dockerfile"
